/-
  Generated by lsc — Lean types and pure function mirrors.
-/
import LemmaScript

namespace Pure

def isNAType (t : Int) : Bool :=
  t = 13

def isErrorType (t : Int) : Bool :=
  t = 10 ∨ t = 11 ∨ t = 12

def arithType (a : Int) (b : Int) : Int :=
  if a = 10 ∨ a = 11 ∨ a = 12 then
    a
  else
    if b = 10 ∨ b = 11 ∨ b = 12 then
      b
    else
      if a = 1 ∨ b = 1 then
        12
      else
        0

def divType (a : Int) (b : Int) (bNum : Int) : Int :=
  let lattice := arithType a b
  if lattice = 10 ∨ lattice = 11 ∨ lattice = 12 then
    lattice
  else
    if bNum = 0 then
      11
    else
      0

def concatType (a : Int) (b : Int) : Int :=
  if a = 10 ∨ a = 11 ∨ a = 12 then
    a
  else
    if b = 10 ∨ b = 11 ∨ b = 12 then
      b
    else
      1

end Pure
