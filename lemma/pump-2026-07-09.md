# LemmaScript / Leanstral pump — 2026-07-09

## Dafny pump
- Strengthened pure-facade `ensures` on clamp/bounds/offset/applyAxisOffset.
- Added `wouldOffsetRef` with iff vs `offsetCol`/`offsetRow` sentinels.
- `offsetRelativeA1`: proved `wouldOffsetRef ==> "#REF!"` in Dafny.
- Absolute-axis: `offsetA1Parts` / `offsetA1` / `formatA1Parts` for `$A1`/`A$1`/`$A$1`.
- Structural: `adjustAxis` / `adjustA1` for delete-band `#REF!` + insert shift.
- Structural REF policy: `wouldAdjustRef` iff + `adjustA1` implication; stronger
  `adjustAxis` left-of-start / delete-band ensures.
- Absolute REF policy: `wouldOffsetA1Ref` iff `offsetA1Parts.col === -1`;
  `offsetA1Parts` joint sentinel `col === -1 <=> row === -1`.
- rcColname ranks: `colFromRcRanks` / `colToRcRanks` (quotient form, not letter index).
- `formatA1Parts` OOB ⇒ `#REF!`.
- String converse (`!== "#REF!"`) not Dafny-proved for multi-char builders
  (crToCoord is a method; char-index gen is type-broken). Bun locks converses.
- **26 verified / 0 errors** on `lemma/a1.dfy`.

## Leanstral counterexample scout (`labs-leanstral-1-5-1`)
Suggested edges (promoted into Bun oracle matrix where applicable):
- offsetCol(702,1) → -1
- offsetRow(1,-1) → -1
- wouldOffsetRef(702,1,1,0) → true
- offsetRelativeA1 ZZ1 + col → #REF!
- applyAxisOffset absolute lock

## Lean track
- `verify:lean:gen` green (types/def for expanded facade).
- Import graph (no hand-edit of generated files):
  `a1.types` → `a1.spec` (pure lemmas) → `a1.def` (gen imports spec) → `a1.proof`.
- `a1.spec.lean` must **not** import `a1.def` (would cycle with lsc gen).
- LETTERS length lemmas live in `a1.proof.lean`; toString/dollar lemmas in `a1.spec.lean`.
- Full `prove_correct` suite including `wouldOffsetA1Ref` / ranks — **no `sorry`**.
- `bun run verify:lean:gen && lake build «a1.proof»` stays green without patching
  `a1.def.lean`.
- Optional `verify:lean:build` needs sibling Loom/Velvet/LemmaScript.

## Commands
```bash
bun run verify:dafny:regen   # after facade edits (not plain gen alone)
# if regen merge is dirty after force-gen: cp lemma/a1.dfy.gen lemma/a1.dfy
bun run verify:both
bun test test/lemma-a1-facade.test.ts
```
