# LemmaScript / Leanstral pump — 2026-07-09

## Dafny pump
- Strengthened pure-facade `ensures` on clamp/bounds/offset/applyAxisOffset.
- Added `wouldOffsetRef` with iff vs `offsetCol`/`offsetRow` sentinels.
- `offsetRelativeA1`: proved `wouldOffsetRef ==> "#REF!"` in Dafny (16 VCs / 0 errors).
- Converse (`!wouldOffsetRef ==> not "#REF!"`) **not** Dafny-proved (would need
  `crToCoord` never emits `#REF!`); locked by Bun iff matrix instead.

## Leanstral counterexample scout (`labs-leanstral-1-5-1`)
Suggested edges (all promoted into Bun oracle matrix where applicable):
- offsetCol(702,1) → -1
- offsetRow(1,-1) → -1
- wouldOffsetRef(702,1,1,0) → true
- offsetRelativeA1 ZZ1 + col → #REF!
- applyAxisOffset absolute lock

## Commands
```bash
bun run verify:dafny:regen   # after facade edits (not plain gen alone)
bun run verify:both
bun test test/lemma-a1-facade.test.ts
```
