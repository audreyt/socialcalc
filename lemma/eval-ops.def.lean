/-
  Generated by lsc from eval-ops.ts
  Do not edit — re-run `lsc gen` to regenerate.
-/
import «eval-ops.types»

set_option loom.semantics.termination "total"
set_option loom.semantics.choice "demonic"

method isNAType (t : Int) return (res : Bool)
  ensures res = true ∨ res = false
  ensures res = true ↔ t = 13
  do
    return Pure.isNAType t

method isErrorType (t : Int) return (res : Bool)
  ensures res = true ∨ res = false
  ensures res = true ↔ t = 10 ∨ t = 11 ∨ t = 12
  do
    return Pure.isErrorType t

method arithType (a : Int) (b : Int) return (res : Int)
  ensures Pure.isErrorType a = true → res = a
  ensures Pure.isErrorType a = false → Pure.isErrorType b = true → res = b
  ensures Pure.isErrorType a = false → Pure.isErrorType b = false → a = 1 ∨ b = 1 → res = 12
  ensures Pure.isErrorType a = false → Pure.isErrorType b = false → a ≠ 1 → b ≠ 1 → res = 0
  do
    return Pure.arithType a b

method divType (a : Int) (b : Int) (bNum : Int) return (res : Int)
  ensures Pure.isErrorType a = true → res = a
  ensures Pure.isErrorType a = false → Pure.isErrorType b = true → res = b
  ensures Pure.isErrorType a = false → Pure.isErrorType b = false → a = 1 ∨ b = 1 → res = 12
  ensures a = 0 → b = 0 → bNum = 0 → res = 11
  ensures a = 0 → b = 0 → bNum ≠ 0 → res = 0
  do
    return Pure.divType a b bNum

method concatType (a : Int) (b : Int) return (res : Int)
  ensures Pure.isErrorType a = true → res = a
  ensures Pure.isErrorType a = false → Pure.isErrorType b = true → res = b
  ensures Pure.isErrorType a = false → Pure.isErrorType b = false → res = 1
  do
    return Pure.concatType a b
