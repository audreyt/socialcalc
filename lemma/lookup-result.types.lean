/-
  Generated by lsc — Lean types and pure function mirrors.
-/
import LemmaScript

namespace Pure

def resolveToken (type1 : String) (type2 : String) (token : String) : String :=
  if token = "1" then
    type1
  else
    if token = "2" then
      type2
    else
      token

end Pure
